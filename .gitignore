# Zig build artifacts
.zig-cache/
zig-out/
main

# Zig package manager's fetched dependency sources (regenerated by `zig build`)
zig-pkg/

# Local docs/notes not tracked
docs/
digram.canvas

# octo tool's own dogfooded repo state (not the real git history)
.octo/

# Claude Code local settings
.claude/

# Python prototype build/runtime artifacts
pythonPrototype/venv/
pythonPrototype/build/
pythonPrototype/dist/
pythonPrototype/__pycache__/
pythonPrototype/graphify-out/

# C++ test build artifacts
test/build/
test/octo_test
